# Claude Code Session

| Field | Value |
|---|---|
| **Project** | `c:\mobile2-unitask` |
| **Session ID** | `55235586-8ede-4ba3-8a59-b0c5f1b81a6b` |
| **Working Dir** | `C:\mobile2\unitask` |
| **Started** | 2026. 5. 16. 오후 4:56:00 |
| **Last Updated** | 2026. 5. 16. 오후 4:56:56 |
| **Messages** | 6 |

---

## User <sup>2026. 5. 16. 오후 4:56:00</sup>

@lib\ 여기 폴더구조 설명해줄래?


---

## Assistant <sup>2026. 5. 16. 오후 4:56:07</sup>


---

## Assistant <sup>2026. 5. 16. 오후 4:56:08</sup>

`lib` 폴더 구조를 살펴볼게요.


---

## Assistant <sup>2026. 5. 16. 오후 4:56:11</sup>

<details>
<summary>Tool: <code>Agent</code></summary>

```json
{
  "description": "Explore lib folder structure",
  "subagent_type": "Explore",
  "prompt": "Flutter 프로젝트의 lib 폴더 구조를 탐색해주세요. 경로는 C:\\mobile2\\unitask\\lib 입니다. 모든 하위 폴더와 파일 목록을 트리 형태로 파악해주세요. 각 파일의 내용을 간략히 읽어서 어떤 역할을 하는지도 파악해주세요. thoroughness: very thorough"
}
```

</details>


---

## User <sup>2026. 5. 16. 오후 4:56:43</sup>

<details>
<summary>Tool Result</summary>

```
Perfect! Now I have a complete understanding of the project structure. Let me compile a comprehensive report.

## Flutter 프로젝트 lib 폴더 구조 분석

### 디렉토리 트리 구조

```
lib/
├── main.dart
├── app/
│   ├── app_strings.dart
│   ├── router/
│   │   ├── app_page.dart
│   │   └── app__router.dart
│   └── theme/
│       └── app_theme.dart
├── core/
│   ├── extensions/
│   │   ├── build_context_extension.dart
│   │   └── sized_box_extension.dart
│   └── models/
│       └── result.dart
├── features/
│   └── auth/
│       └── auth_provider.dart
├── models/
│   └── auth_data.dart
├── services/
│   └── auth_api_service.dart
└── ui/
    ├── common/
    │   ├── label_text_field.dart
    │   └── text_divider.dart
    └── pages/
        ├── home/
        │   └── home_page.dart
        ├── login/
        │   └── login_page.dart
        └── signup/
            └── signup_page.dart
```

---

### 파일별 상세 역할 설명

#### **최상위 계층**

**main.dart** - 애플리케이션 진입점
- ProviderScope로 감싼 UniTaskApp 생성
- MaterialApp.router를 사용한
...(truncated)
```

</details>


---

## Assistant <sup>2026. 5. 16. 오후 4:56:56</sup>

`lib` 폴더 구조를 정리해드릴게요.

---

## 폴더 구조

```
lib/
├── main.dart                  # 앱 진입점
├── app/                       # 앱 전체 설정
│   ├── app_strings.dart       # 상수 (API URL 등)
│   ├── router/                # 라우팅 설정
│   │   ├── app_page.dart      # 페이지 enum (login, signup, home)
│   │   └── app__router.dart   # GoRouter 라우터
│   └── theme/
│       └── app_theme.dart     # Light/Dark 테마
├── core/                      # 공통 유틸리티
│   ├── extensions/
│   │   ├── build_context_extension.dart  # SnackBar 헬퍼
│   │   └── sized_box_extension.dart      # 간편 SizedBox (16.heightBox 등)
│   └── models/
│       └── result.dart        # Success/Failure sealed class
├── features/
│   └── auth/
│       └── auth_provider.dart # Riverpod 인증 상태 관리
├── models/
│   └── auth_data.dart         # 인증 데이터 모델 (토큰 등)
├── services/
│   └── auth_api_service.dart  # 로그인/회원가입 API 호출
└── ui/
    ├── common/                # 재사용 위젯
    │   ├── label_text_field.dart  # 레이블+아이콘 입력 필드
    │   └── text_divider.dart      # "─── 또는 ───" 구분선
    └── pages/
        ├── home/
        │   └── home_page.dart     # 메인 화면 (개발 중)
        ├── login/
        │   └── login_page.dart    # 로그인 화면
        └── signup/
            └── signup_page.dart   # 회원가입 화면
```

---

## 레이어 역할 요약

| 폴더 | 역할 |
|------|------|
| `app/` | 라우팅, 테마, 전역 상수 |
| `core/` | 프로젝트 전반에서 쓰이는 유틸 (확장함수, Result 패턴) |
| `features/` | 기능 단위 비즈니스 로직 (Riverpod Provider) |
| `models/` | 데이터 모델 클래스 |
| `services/` | API 통신 담당 |
| `ui/` | 화면 및 재사용 위젯 |

---

현재 **로그인/회원가입** 기능은 완성된 상태이고, **홈 화면**은 `Placeholder`만 있어 개발 중입니다. 기술 스택은 Riverpod + GoRouter + http 패키지 조합이에요.


---
