# Claude Code Session

| Field | Value |
|---|---|
| **Project** | `c:\mobile2-unitask` |
| **Session ID** | `82534d69-1bed-4795-8b7d-ddbe01421d0a` |
| **Working Dir** | `C:\mobile2\unitask` |
| **Started** | 2026. 5. 14. 오후 4:33:37 |
| **Last Updated** | 2026. 5. 14. 오후 4:34:02 |
| **Messages** | 10 |

---

## User <sup>2026. 5. 14. 오후 4:33:37</sup>

오늘 한 기록 너가 알 수 있니 저 깃에서 커밋할만한 파일들 보고? 여튼 오늘 뭐 했는지 요약 가능하니


---

## Assistant <sup>2026. 5. 14. 오후 4:33:42</sup>


---

## Assistant <sup>2026. 5. 14. 오후 4:33:42</sup>

git 변경사항과 수정된 파일들을 살펴볼게요.


---

## Assistant <sup>2026. 5. 14. 오후 4:33:44</sup>

<details>
<summary>Tool: <code>Bash</code></summary>

```json
{
  "command": "git diff HEAD -- lib/main.dart lib/ui/pages/home/home_page.dart lib/ui/pages/login/login_page.dart lib/ui/pages/signup/signup_page.dart pubspec.yaml 2>/dev/null | head -300",
  "description": "Show git diff for modified files"
}
```

</details>


---

## Assistant <sup>2026. 5. 14. 오후 4:33:45</sup>

<details>
<summary>Tool: <code>Bash</code></summary>

```json
{
  "command": "find lib/core lib/features -type f 2>/dev/null && cat lib/app/app_strings.dart 2>/dev/null && cat lib/services/auth_api_service.dart 2>/dev/null",
  "description": "Show new files structure and content"
}
```

</details>


---

## User <sup>2026. 5. 14. 오후 4:33:46</sup>

<details>
<summary>Tool Result</summary>

```
diff --git a/lib/main.dart b/lib/main.dart
index 5e66880..f35a94a 100644
--- a/lib/main.dart
+++ b/lib/main.dart
@@ -1,10 +1,11 @@
 import 'package:flutter/material.dart';
+import 'package:flutter_riverpod/flutter_riverpod.dart';
 import 'package:unitask/app/theme/app_theme.dart';
 
 import 'app/router/app__router.dart';
 
 void main() {
-  runApp(const UniTaskApp());
+  runApp(const ProviderScope(child: UniTaskApp()));
 }
 
 class UniTaskApp extends StatelessWidget {
diff --git a/lib/ui/pages/home/home_page.dart b/lib/ui/pages/home/home_page.dart
index 96c8a94..c1a13ee 100644
--- a/lib/ui/pages/home/home_page.dart
+++ b/lib/ui/pages/home/home_page.dart
@@ -5,6 +5,7 @@ class HomePage extends StatelessWidget {
   const HomePage({super.key});
   @override
   Widget build(BuildContext context) {
+    //뒤로가기 방지 / 감지
     return PopScope(
       canPop: false,
       onPopInvokedWithResult: (didPop, result) {
diff --git a/lib/ui/pages/login/login_page.dart b/lib/ui/pages/login/login_page.da
...(truncated)
```

</details>


---

## User <sup>2026. 5. 14. 오후 4:33:46</sup>

<details>
<summary>Tool Result</summary>

```
lib/core/extensions/build_context_extension.dart
lib/core/extensions/sized_box_extension.dart
lib/core/models/result.dart
lib/features/auth/auth_provider.dart
class AppStrings {
  static const apiHostUrl = 'https://daelim.fleecy.dev/functions/v1';
}
import 'dart:convert';

import 'package:flutter/material.dart';
import 'package:http/http.dart' as http;
import 'package:unitask/app/app_strings.dart';
import 'package:unitask/core/models/result.dart';
import 'package:unitask/models/auth_data.dart';

class AuthApiService {
  final String _signupUrl = '${AppStrings.apiHostUrl}/students/signup';
  final String _loginUrl = '${AppStrings.apiHostUrl}/students/login';

  Future<Result<void>> signup({
    required String email,
    required String password,
    required String name,
  }) async {
    try {
      final response = await http.post(
        Uri.parse(_signupUrl),
        body: jsonEncode({'email': email, 'password': password, 'name': name}),
      );

      fi
...(truncated)
```

</details>


---

## Assistant <sup>2026. 5. 14. 오후 4:33:50</sup>

<details>
<summary>Tool: <code>Bash</code></summary>

```json
{
  "command": "cat lib/features/auth/auth_provider.dart && cat lib/core/models/result.dart",
  "description": "Show auth provider and result model"
}
```

</details>


---

## User <sup>2026. 5. 14. 오후 4:33:50</sup>

<details>
<summary>Tool Result</summary>

```
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:unitask/core/models/result.dart';
import 'package:unitask/models/auth_data.dart';
import 'package:unitask/services/auth_api_service.dart';

final authApiServiceProvider = Provider<AuthApiService>(
  (ref) => AuthApiService(),
);

final authProvider = NotifierProvider<AuthProvider, AsyncValue<AuthData?>>(
  AuthProvider.new,
);

class AuthProvider extends Notifier<AsyncValue<AuthData?>> {
  @override
  AsyncValue<AuthData?> build() => const AsyncData(null);

  Future<Result<void>> signup({
    required String email,
    required String password,
    required String name,
  }) async {
    final authData = state.value;
    state = const AsyncLoading();
    try {
      final result = await ref
          .read(authApiServiceProvider)
          .signup(email: email, password: password, name: name);

      state = switch (result) {
        Success() => AsyncData(authData),
        Failure(:final exception) => AsyncError(
...(truncated)
```

</details>


---

## Assistant <sup>2026. 5. 14. 오후 4:34:02</sup>

오늘 작업 요약이에요:

---

## 오늘 한 것 요약

### 1. 상태관리 라이브러리 교체: `provider` → `flutter_riverpod`
- `pubspec.yaml`에서 `provider`를 `flutter_riverpod ^3.3.1`로 교체
- `main.dart`에 `ProviderScope`로 앱 감싸기

### 2. 아키텍처 리팩터링 (폴더 구조 개선)
- `lib/core/` 폴더 새로 만들고 extensions, models 이동
  - `sized_box_extension` → `lib/core/extensions/`
  - `snackbar_extension` → `lib/core/extensions/build_context_extension.dart`로 이동/개선
- `lib/features/auth/` 폴더 생성
- 기존 `api_service.dart` 삭제하고 `auth_api_service.dart`로 분리

### 3. Result 타입 도입
- `sealed class Result<T>` / `Success` / `Failure` 패턴 적용
- API 에러 처리를 명시적으로 관리

### 4. AuthProvider (Riverpod Notifier) 작성
- `AuthProvider`가 로그인/회원가입 상태(`AsyncValue<AuthData?>`) 관리
- 로딩 상태, 에러 상태 자동 처리

### 5. LoginPage / SignupPage Riverpod으로 전환
- `StatefulWidget` → `ConsumerStatefulWidget`
- 직접 API 호출하던 것 → `ref.read(authProvider.notifier).login/signup()` 으로 변경
- 로그인 버튼에 로딩 스피너(`CircularProgressIndicator`) 추가
- `switch (result)` 패턴으로 성공/실패 분기 처리

---

한마디로: **"날것의 직접 API 호출 코드"를 "Riverpod 기반의 구조화된 상태관리 코드"로 리팩터링한 날**이었어요.


---
